import{news}from'../data/mockData';export default function News(){const local=JSON.parse(localStorage.getItem('news')||'[]');return <><h1>Cricket News</h1><div className="grid">{[...local,...news].map((n,i)=><article className="card news" key={i}><span className="tag">{n.tag||'News'}</span><h3>{n.title}</h3><p>{n.body||'Latest cricket update with match insights, team changes and expert analysis.'}</p></article>)}</div></>}
