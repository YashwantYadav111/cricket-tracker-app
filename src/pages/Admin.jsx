import{useState}from'react';export default function Admin(){const[title,setTitle]=useState('');const[body,setBody]=useState('');const save=()=>{const old=JSON.parse(localStorage.getItem('news')||'[]');localStorage.setItem('news',JSON.stringify([{title,body,tag:'Admin Update'},...old]));setTitle('');setBody('');alert('News saved locally!')};return <div className="admin card"><h1>Admin Panel</h1><p className="sub">PHP/MySQL removed. This demo saves news in browser localStorage.</p><input placeholder="News title" value={title} onChange={e=>setTitle(e.target.value)}/><textarea placeholder="News description" value={body} onChange={e=>setBody(e.target.value)}></textarea><button className="btn" onClick={save}>Publish News</button></div>}
