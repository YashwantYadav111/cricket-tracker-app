import{useEffect,useState}from'react';import MatchCard from '../components/MatchCard';import{getLiveMatches}from'../services/cricketApi';
export default function Live(){const[matches,setMatches]=useState([]);const[loading,setLoading]=useState(true);useEffect(()=>{const load=()=>getLiveMatches().then(d=>{setMatches(d);setLoading(false)});load();const t=setInterval(load,30000);return()=>clearInterval(t)},[]);return <><h1>Live Cricket Score</h1><p className="sub">Auto refresh every 30 seconds.</p>{loading?<div className="loader">Loading live scores...</div>:<div className="grid">{matches.map(m=><MatchCard key={m.id} m={m}/>)}</div>}</>}
